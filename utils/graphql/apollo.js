import ApolloClient from 'apollo-boost';
export default new ApolloClient({ uri: 'http://localhost:1337/graphql' });

// import getConfig from 'next/config';

// const { publicRuntimeConfig } = getConfig();

// Apollo Client
