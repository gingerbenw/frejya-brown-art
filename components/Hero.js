import React from 'react';
import styled from 'styled-components';

const Hero = () => (
  <HeroWrapper>
    {/* <AsyncLoadBackground src={} /> */}
    <h1>Freya Brown Art</h1>
  </HeroWrapper>
);

export default Hero;

const HeroWrapper = styled.div``;
